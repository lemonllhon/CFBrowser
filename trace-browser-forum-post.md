# Trace Browser 官网介绍：让浏览器环境可隔离、可控制、可自动化

如果你正在使用多个浏览器账号、管理不同地区的网络环境，或者需要把浏览器操作交给自动化流程，欢迎了解一下 Trace Browser。

官网地址：<https://browser.lemon.vin>

## Trace Browser 是什么？

Trace Browser 是一个桌面端、本地优先的浏览器工作台。

它把浏览器指纹实例、代理资源、Cookie、扩展、浏览器内核、自动化工作流、备份恢复和外部脚本接入，统一放进一个可管理的工作区里。

产品希望解决的问题很简单：

> 每个浏览器环境都应该独立、清晰、可复用，也可以随时交接和恢复。

## 官网重点展示的 09 项能力

1. **环境隔离**：每个账号使用独立的用户数据、设备画像、Cookie、扩展和启动配置。
2. **统一运维**：集中管理实例、代理、扩展、浏览器内核和运行状态。
3. **随时流转**：支持备份、恢复、同步、分享和实例交接，环境可以从一个设备迁移到另一个设备。
4. **自动化编排**：通过 RPA 工作流设计器编排节点、条件、循环、选择器和数据处理步骤。
5. **代理与出口**：导入代理和订阅，进行测速、健康检查、分流、链式代理和故障切换。
6. **数据采集**：支持列表、详情、SKU、文本、属性和资源 URL 采集，并导出 JSON、CSV 或文本。
7. **外部接入**：通过 HTTP API、Launch Code、CDP 以及 Playwright、Puppeteer、Selenium 等工具接管实例。
8. **持续维护**：管理内核、扩展、日志、备份、配置和运行历史，让环境长期保持可维护。
9. **全平台支持**：覆盖 Windows、macOS 和 Linux，并按不同架构提供对应版本。

## 核心功能

### 1. 指纹浏览器实例

实例是 Trace Browser 中最小、最清晰的工作单元。

- 创建、编辑、复制、删除、启动、停止、重启和查看详情
- 配置浏览器品牌、操作系统、语言、地区、时区、分辨率、CPU、内存等设备画像
- 支持 WebGL、Canvas、Audio、WebRTC、媒体设备、字体等指纹配置
- 为实例绑定代理、Cookie、扩展、标签、分组和启动页
- 支持 Launch Code、调试端口、CDP 地址和运行状态查看
- 支持批量启动、批量停止、筛选、排序和并发控制

### 2. 代理池与网络路由

代理不再只是粘贴进去的一段文本，而是可以检测、分配和追踪的网络资源。

- 支持 HTTP、HTTPS、SOCKS5 代理
- 支持订阅 URL、Clash YAML、Base64、vmess、vless、trojan、Shadowsocks、hysteria2、AnyTLS 等内容导入
- 支持单节点和批量测速、IP 健康检查、延迟和风险信息查看
- 支持代理分组、代理池、手动分配和自动分配
- 支持域名、路径、地区、健康状态等路由规则
- 支持前置代理、链式代理、故障切换和命中记录

### 3. Cookie、快照与备份恢复

登录状态和浏览器环境可以被保存、恢复和迁移。

- 支持持久 Cookie 和运行中 Cookie 快照
- 支持 Cookie 查看、清理、导入和导出
- 支持全量配置备份和实例级备份
- 备份内容可包含实例、代理、路由、扩展绑定、标签、分组、默认内容和 RPA 数据
- 导入前进行校验，冲突实例可以自动重命名
- 支持云端备份、恢复、设备同步和实例分享

### 4. 浏览器内核与扩展管理

浏览器内核和插件也纳入统一工作区管理。

- 扫描和注册本地 Chrome / Chromium 内核
- 查看内核版本、架构、路径、状态和实例使用数量
- 支持从官方下载页、自定义地址或内部镜像下载内核
- 支持 ZIP、tar.xz、AppImage、DMG 等常见包格式
- 支持 ZIP、CRX、目录、地址和拖拽导入扩展
- 查看扩展版本、权限、安装目录和绑定实例
- 支持共享绑定、独占绑定、自动绑定和源实例数据同步

## RPA 自动化工作流

Trace Browser 的自动化模块不只是简单录制点击，而是面向可编辑、可调试、可复用流程设计的工作流系统。

### 工作流设计器

- 拖拽节点和连线
- 普通、成功、失败连线
- 真 / 假分支和循环回路
- 撤销、重做、缩放、平移和画布状态恢复
- 工作流变量、触发器、导入、导出和结构校验
- 工作流片段、模块组和节点包复用

### 页面交互与选择器

- 点击、输入、滚动、悬停、按键、上传、下载和截屏
- 支持 CSS Selector、XPath、iframe 和 Shadow DOM
- 支持元素、列表、字段和表单控件拾取
- 展示匹配数量、稳定性评分和历史选择器
- 录制页面访问、点击、输入、选择、滚动、键盘、下载和截图

### 数据处理与导出

- 读取文本、HTML、属性、链接、图片和资源 URL
- 支持列表到详情、SKU 和变体组合
- 支持变量映射、正则、等待条件和失败策略
- 支持 JSON、CSV、文本导出
- 支持字段顺序、分隔符、BOM、模板和冲突处理

### 任务与运行记录

- 立即执行、定时执行、循环任务和 Cron 计划
- 配置开始时间、间隔、最大并发和运行实例
- 支持暂停、恢复、取消、重试和清理历史
- 查看节点输入、输出、状态、耗时、失败 URL、错误堆栈和截图
- 按工作流、实例、状态和时间筛选运行历史

## 外部系统接入

Trace Browser 提供本地 HTTP 与 CDP 接入能力，方便把浏览器实例交给自己的脚本或调度系统。

- 默认本地服务地址：`127.0.0.1:19876`
- 支持 `/api/health` 健康检查
- 支持查询、创建、更新和删除实例
- 支持通过实例 ID、名称、关键字、标签、分组或 Launch Code 启动实例
- 返回实例 ID、PID、调试端口、`cdpUrl` 和运行警告
- 可接入 Playwright、Puppeteer、Selenium、Python、Node.js、PowerShell 和 cURL
- 默认仅允许本机访问，并提供端口、CORS、鉴权和超时配置

## 官网还提供了什么？

官网不只是一个下载页，也提供了完整的产品能力说明：

- 工作台、实例、代理、Cookie、备份和自动化的功能分区
- 21 项详细能力索引，可逐项查看支持的功能和使用边界
- 四种典型使用方式：手动管理、多账号运营、RPA 自动化、外部系统接入
- 七种主题配色：草木灰、落晖黄、远山绿、天空蓝、朱砂红、烟墨紫、暗夜黑
- 动态主题预览和自动切换展示
- 产品截图、流程说明、运行状态和下载入口

## 支持的平台

- **Windows**：x64 / ARM64，提供安装版和便携版
- **macOS**：Apple 芯片 / Intel，提供 DMG 安装版
- **Linux**：amd64 / arm64，提供 DEB 安装版和 TAR.GZ 便携包

官网会通过统一下载入口匹配最新发布版本，用户可以按平台、架构和安装方式选择对应包。

## 适合哪些场景？

- 多账号运营和账号环境隔离
- 海外站点访问与地区化浏览器环境管理
- 电商、广告、内容、社媒等多实例运营
- 网站测试、回归测试和多浏览器环境验证
- 浏览器自动化、数据采集和批量任务
- 浏览器环境备份、换机、迁移和团队交接
- 需要通过 API、CDP 或自研调度器接管浏览器的开发者

## 使用边界

Trace Browser 提供浏览器环境管理、自动化和接入能力，但不会自动消除网络供应商、站点风控、验证码、第三方 API 授权或合规责任。

实际运行效果会受到设备资源、代理质量、目标网站、页面结构、并发量和第三方服务状态影响。涉及账号、Cookie、邮箱凭据、代理和云端同步数据时，请在可信环境中使用，并遵循所在组织的安全策略和相关法律法规。

## 欢迎体验

如果你希望把浏览器环境从“临时打开一个窗口”，变成“可以隔离、管理、自动化、备份和复用的工作单元”，可以访问官网了解完整能力：

**官网：<https://browser.lemon.vin>**

欢迎反馈你最常用的浏览器场景，以及你希望 Trace Browser 下一步补齐的能力。
